@{
    Layout = null;
}


@*<link href="https://editor.datatables.net/extensions/Editor/js/dataTables.editor.min.js" rel="stylesheet" />
    <link href="https://cdn.datatables.net/select/1.3.0/js/dataTables.select.min.js" rel="stylesheet" />
    <link href="https://cdn.datatables.net/buttons/1.5.6/js/dataTables.buttons.min.js" rel="stylesheet" />
    <link href="https://cdn.datatables.net/1.10.19/js/jquery.dataTables.min.js" rel="stylesheet" />*@


<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
<link href="~/Content/bootstrap.min.css" rel="stylesheet">
<link href="https://cdn.datatables.net/v/dt/dt-1.10.18/datatables.min.css" rel="stylesheet" />
<script type="text/javascript">

</script>
<style>
    .btn {
        margin-right: 20px;
    }
</style>
<div class="container">
    <h2>Quản lý Upload Thesis</h2>
    <div>
        <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#modalEssay">
            Open modal
        </button>
        <button id="btnEdit" type='button' class='btn btn-danger'>Sửa</button>
        <button id="btnDel" type='button' class='btn btn-danger'>Xóa</button>
    </div>


    <table id="loadsubject" class="table table-bordered table-striped dataTable no-footer" role="grid" aria-describedby="crudTable_info">
        <thead>
            <tr>
                <th>ID</th>
                <th>Mã khóa luận</th>
                <th>Tiêu đề</th>
                <th>GVHD</th>
                <th>Sinh viên 1</th>
                <th>Sinh viên 2</th>
                <th>Mô tả</th>
                <th>Tên file</th>
                <th>Ngày Up</th>
            </tr>
        </thead>
        <tbody></tbody>
    </table>
</div>
<!--Popup edit-->
<div class="modal" id="EditSub">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title">Sửa môn học</h4>
                <button type="button" id="btn_closeX" class="close" data-dismiss="modal">&times;</button>
            </div>
            <!-- Modal body -->
            <!-- Sửa môn học -->
            <div id="modal" class="modal-body">
                <div class="form-group">
                    <label for="text">Tên môn:</label>
                    <input type="text" class="form-control" id="EditNameSub_Eb" placeholder="Tên môn" name="Name">
                </div>
                <div style="margin-top:20px">
                    <button id="btnSaveEdit" type="button" class="btn btn-primary">Update</button>
                </div>
            </div>
            <!-- Modal footer -->
            <div class="modal-footer">
                <button type="button" id="btn_closeEdit" class="btn btn-danger btn_close" data-dismiss="modal">Đóng</button>
            </div>

        </div>
    </div>
</div>
<!-- Upload tiểu luận-->
<div class="modal" id="modalEssay">
    <div class="modal-dialog">
        <div class="modal-content">

            <!-- Modal Header -->
            <div class="modal-header">
                <h4 class="modal-title">Upload</h4>
                <button type="button" class="close" data-dismiss="modal">&times;</button>
            </div>

            <!-- Form Upload Ebook -->
            <div class="modal-body">
                <form>
                    <div class="form-group">
                        <label for="text">Tiêu đề:</label>
                        <input type="text" class="form-control" id="titleT" placeholder="Tiêu đề" name="title">

                    </div>
                    <div class="form-group">
                        <label for="text">Giới thiệu:</label>
                        <input type="text" class="form-control" id="describeT placeholder="Giới thiêu" name="describe">

                    </div>
                    <div class="form-group">
                        <label for="text">GVHD:</label>
                        <input type="text" class="form-control" id="instructorT" placeholder="GVHD" name="instructor">

                    </div>
                    <div class="form-group">
                        <label for="text">Sinh viên  1:</label>
                        <input type="text" class="form-control" id="student1T" placeholder="Sinh viên 1" name="student1">

                    </div>
                    <div class="form-group">
                        <label for="text">Sinh viên 2:</label>
                        <input type="text" class="form-control" id="student2T" placeholder="Sinh viên 2" name="student2">

                    </div>
                    @*<div class="form-group">
                            <label for="text">Khóa:</label>
                            <input type="text" class="form-control" id="yearE" placeholder="Năm" name="year">

                        </div>*@
                    <div class="form-group">
                        <label for="text">Bộ môn:</label>
                        <select id="loadSubjectOfEbook" class="form-control"></select>
                    </div>
                    <div class="form-group">
                        <label for="text">Browser</label>

                        <input type="file" name="Files" multiple id="fileInputT" />
                    </div>

                    <div id="error"></div>
                    <div style="margin-top:20px">
                        <button id="uploadEssay" type="button" class="btn btn-primary">Upload</button>
                    </div>
                </form>
            </div>

            <!-- Modal footer -->
            <div class="modal-footer">
                <button type="button" class="btn btn-danger" data-dismiss="modal">Close</button>
            </div>

        </div>
    </div>
</div>

<script src="~/Scripts/jquery-3.3.1.js"></script>
<script src="~/Content/Datatables/jquery.dataTables.min.js"></script>
<script src="~/Scripts/jquery.signalR-2.4.0.min.js"></script>
<!--Reference the autogenerated SignalR hub script. -->
<script src="~/signalr/hubs"></script>

<!--Xóa môn-->
<script>
    //hàm xóa
    function DeleteThesis(id) {
        var r = confirm("Bạn có chắc muốn xóa không?");

        if (r == true) {
            var data = {};
            data.id = id;
            $.ajax({
                type: "POST",
                url: "/api/ManageOfAdminAPI/DeleteFileUploadThesis",
                data: JSON.stringify(data),
                contentType: "application/json; charset=utf-8",
                dataType: "json",
                success: function (result) {
                    alert(result);
                }
            });
        }
    }

    var index = 0;
    var flag = false;
    $(document).ready(function () {
        //click vào row của datatable
        $('#loadsubject tbody').on('click', 'tr', function () {
            flag = true;
            var table = $('#loadsubject').DataTable();
            var rowCurrent = $(this);
            console.log("index", rowCurrent);
            index = rowCurrent[0].sectionRowIndex;
            console.log("rowCurrent", index);
            if ($(this).hasClass('selected')) {
                $(this).removeClass('selected');
            }
            else {
                table.$('tr.selected').removeClass('selected');
                $(this).addClass('selected');
            }

        });
        //xóa 1 row datatable
        $('#btnDel').click(function () {
            var table = $('#loadsubject').DataTable();
            console.log("aaa", table.rows({ selected: true }).data());
            var rowData = table.rows({ selected: true }).data()[index]['id'];
            console.log("rowData", rowData);
            if (flag == true) {
                DeleteThesis(rowData);
                table.row('.selected').remove().draw(false);
            }
        });
        $('body').on('click', '#btnEdit', function (e) {
            var table = $('#loadsubject').DataTable();
            var name = table.rows({ selected: true }).data()[index]['name'];
            alert(name);
            $("input[id='EditNameSub_Eb']").val(name);
            $("#EditSub").show();
        });
        $("#btnSaveEdit").click(function () {
            var table = $('#loadsubject').DataTable();
            var id = table.rows({ selected: true }).data()[index]['id'];
            var name = table.rows({ selected: true }).data()[index]['name'];
            var data = {};
            data.id = id;
            data.name = name;
            $.ajax({
                type: "POST",
                url: "/api/EbookAPI/EditSubjectById",
                data: JSON.stringify(data),
                contentType: "application/json; charset=utf-8",
                dataType: "json",
                success: function (result) {
                    if (result.isSuccess) {
                        alert("Sửa thành công.");
                        //add row datatable
                        var t = $('#loadsubject').DataTable();
                        //t.row.edit({
                        //    "id": id,
                        //    "name": name
                        //}).draw();
                        alert(index);
                        t.row(index).edit({
                            name: name
                        });
                    } else {
                        alert("Sửa không thành công.");
                    }
                }
            });
        });
    });
</script>
<!--Tạo môn học-->
<script>
    $.ajax({
        type: "GET",
        url: "/api/ManageOfAdminAPI/GetSubjectThesis",
        contentType: "application/json; charset=utf-8",
        dataType: "json",
        success: function (result) {

            $.each(result, function (i, v) {
                //alert(v.name);
                //load các môn của ebook lên thanh danh mục bên trái
                //$("#loadSubjectOfEbook").append("<li id='ebook_" + v.id + "'><a>" + v.name + "</a></li >");
                $("#loadSubjectOfEbook").append("<option value='"+v.id+"'>" + v.name + "</option>");
            });
        }
    });
      $(document).ready(function () {
        //Upload dữ liệu vào Ebook
          $("#uploadEssay").click(function () {
             // var data = new FormData();

              //var files = $("#fileInput").get(0).files;

              //// Add the uploaded image content to the form data collection
              //if (files.length > 0) {
              //    data.append("UploadedFiles", files[0]);
              //}
              //var files = $('#fileInput').prop('files')[0];
              @*var data = {};
              data.title = $("input[id='title']").val();
              data.describe = $("input[id='describe']").val();
              data.author = $("input[id='author']").val();
              data.year = $("input[id='year']").val();
              data.author = $("input[id='author']").val();
              data.year = $("input[id='year']").val();
              data.subid = $("#loadSubjectOfEbook option:selected").val();
              data.userid = @Session["user_id"];
              data.file = files;
              console.log(data);*@
              var files = $("#fileInputT").get(0).files;
            var data = new FormData();

              for (var i = 0; i < files.length; i++) {
                  data.append("fileInput", files[i]);
            }
              //cái này là lấy thông tin trên text box  title là key tiltle.val() là giá trị
              data.append("title", $("#titleT").val());
              data.append("describe", $("#describeT").val());
              data.append("instructor", $("#instructorT").val());
              data.append("student1", $("#student1T").val());
              data.append("student2", $("#student2T").val());
              data.append("userid",@Session["user_id"]);
              data.append("subid", $("#loadSubjectOfEbook option:selected").val());


            console.log(data);
            $.ajax({

                type: "POST",
                url: "/api/ManageOfAdminAPI/UploadThesis",
                dataType: "json",
                contentType: false,
                processData: false,
                data: data,

                //contentType: "application/json; charset=utf-8",
                //dataType: "json",
                success: function (result) {
                    if (result.isSuccess) {
                        alert("Tạo môn thành công.");
                        console.log("aaa", result);
                        //add row datatable
                        var t = $('#loadsubject').DataTable();
                        t.row.add({
                            "id": result.id,
                            "thesis_id": result.thesis_Id,
                            "title": result.title,
                            "instructor": result.instructor,
                            "executor1": result.executor1,
                            "executor2": result.executor2,
                            "describe": result.describe,
                            "filename": result.fileName,
                            "date_upload": result.date_Upload
                        }).draw();
                    } else {
                        alert("Tên môn đã tồn tại! Vui lòng đặt tên khác.");
                    }
                }
            });

        });


    });

</script>
<!--Sửa môn học-->
<script>

</script>
<!--Load môn học-->
<script>
    //load môn học ebook
    $(document).ready(function () {
        $.ajax({
            type: "GET",
            url: "/api/ManageOfAdminAPI/GetThesisByAdmin",
            contentType: "application/json; charset=utf-8",
            dataType: "json",
            success: function (data) {
                InitTable(data);
            }
        });
        function InitTable(data) {
            $('#loadsubject').DataTable({
                data: data,
                "columns": [
                    { "data": "id" },
                    { "data": "thesis_id" },
                    { "data": "title" },
                    { "data": "instructor" },
                    { "data": "executor1" },
                    { "data": "executor2" },
                    { "data": "describe" },
                    { "data": "filename" },
                    { "data": "date_upload" },

                ]
            });
        }
        function clearEditSub() {
            $("input[id='EditNameSub_Eb']").val("");
        }
        function clearAddSub() {
            $("input[id='AddNameSub_Eb']").val("");
        }
        $("#btn_closeX").click(function () {
            $("#EditSub").hide();
            clearEditSub();
        });
        $("#btn_closeEdit").click(function () {
            $("#EditSub").hide();
            clearEditSub();
        });
        //$("#btn_closeX1").click(function () {
        //    $("#AddSub").hide();
        //    clearAddSub();
        //});
        //$("#btn_closeAdd").click(function () {
        //    $("#AddSub").hide();
        //    clearAddSub();
        //});
    });

</script>
