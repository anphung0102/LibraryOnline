@{
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}


@*<link href="https://editor.datatables.net/extensions/Editor/js/dataTables.editor.min.js" rel="stylesheet" />
    <link href="https://cdn.datatables.net/select/1.3.0/js/dataTables.select.min.js" rel="stylesheet" />
    <link href="https://cdn.datatables.net/buttons/1.5.6/js/dataTables.buttons.min.js" rel="stylesheet" />
    <link href="https://cdn.datatables.net/1.10.19/js/jquery.dataTables.min.js" rel="stylesheet" />*@

@*<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>*@
@*<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>*@
@*<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>*@
@*<link href="~/Content/bootstrap.min.css" rel="stylesheet">*@
@*<script type="text/javascript" src="http://ajax.aspnetcdn.com/ajax/jquery.validate/1.13.1/jquery.validate.min.js"></script>
<link href="https://cdn.datatables.net/v/dt/dt-1.10.18/datatables.min.css" rel="stylesheet" />*@


<div class="container" id="manage">
    <h2>Quản lý Users</h2>
    <div>
        <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#modalAddUser">
            Open modal
        </button>
        <button id="btnEdit" type='button' class='btn btn-danger'>Sửa</button>
        <button id="btnDel" type='button' class='btn btn-danger'>Xóa</button>
    </div>


    <table id="loadsubject" class="table table-bordered table-striped dataTable no-footer" role="grid" aria-describedby="crudTable_info">
        <thead>
            <tr>
                <th>ID</th>
                <th>Email</th>
                <th>Mật khẩu</th>
                <th>Quyền</th>
                <th>Họ tên</th>
                <th>MSSV</th>
                <th>Mã lớp</th>

            </tr>
        </thead>
        <tbody></tbody>
    </table>
</div>
<!--Popup edit-->
<div class="modal" id="EditSub">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title">Sửa môn học</h4>
                <button type="button" id="btn_closeX" class="close" data-dismiss="modal">&times;</button>
            </div>
            <!-- Modal body -->
            <!-- Sửa môn học -->
            <div id="modal" class="modal-body">
                <div class="form-group">
                    <label for="text">Tên môn:</label>
                    <input type="text" class="form-control" id="EditNameSub_Eb" placeholder="Tên môn" name="Name">
                </div>
                <div style="margin-top:20px">
                    <button id="btnSaveEdit" type="button" class="btn btn-primary">Update</button>
                </div>
            </div>
            <!-- Modal footer -->
            <div class="modal-footer">
                <button type="button" id="btn_closeEdit" class="btn btn-danger btn_close" data-dismiss="modal">Đóng</button>
            </div>

        </div>
    </div>
</div>
<!-- Upload tiểu luận-->
<div class="modal" id="modalAddUser">
    <div class="modal-dialog">
        <div class="modal-content">

            <!-- Modal Header -->
            <div class="modal-header">
                <h4 class="modal-title">User</h4>
                <button type="button" class="close" data-dismiss="modal">&times;</button>
            </div>

            <!-- Form Upload Ebook -->
            <div class="modal-body">

                <form accept-charset="UTF-8" id="submit_addUser" class="was-validated" role="form">
                    <div class="form-group">
                        <label for="text">Email:</label>
                        <input type="email" class="form-control" id="email" placeholder="Email" name="email" required>
                        <div class="invalid-feedback">Please fill out this field.</div>
                    </div>
                    <div class="form-group">
                        <label for="text">Password:</label>
                        <input type="text" class="form-control" id="password" placeholder="Mật khẩu" name="password" required>
                    </div>
                    <div class="form-group">
                        <label for="text">Quyền:</label>
                        <input type="text" class="form-control" id="role" placeholder="Giới thiêu" name="role" required>

                    </div>
                    <div class="form-group">
                        <label for="text">Họ tên:</label>
                        <input type="text" class="form-control" id="name" placeholder="Name" name="name" required>

                    </div>
                    <div class="form-group">
                        <label for="text">MSSV:</label>
                        <input type="number" class="form-control" id="studentid" placeholder="MSSV" name="mssv" required>

                    </div>
                    <div class="form-group">
                        <label for="text">Mã lớp:</label>
                        <input type="text" class="form-control" id="classid" placeholder="Mã lớp" name="malop" required>

                    </div>
                    <div id="error"></div>
                    @Html.ValidationSummary()
                    <div style="margin-top:20px">
                        <button id="addUser" type="button" class="btn btn-primary"><i class="icon-ok"></i>Add<span class="loading"></span></button>
                    </div>
                </form>
            </div>

            <!-- Modal footer -->
            <div class="modal-footer">
                <button type="button" class="btn btn-danger" data-dismiss="modal">Close</button>
            </div>

        </div>
    </div>
</div>

@*<script src="~/Scripts/jquery-3.3.1.js"></script>*@
<script src="~/Content/Datatables/jquery.dataTables.min.js"></script>

<script src="~/Scripts/jquery.signalR-2.4.0.min.js"></script>
<!--Reference the autogenerated SignalR hub script. -->
<script src="~/signalr/hubs"></script>

<!--Xóa môn-->
<script>
    //hàm xóa
    function DeleteUser(id) {
        var r = confirm("Bạn có chắc muốn xóa không?");

        if (r == true) {
            var data = {};
            data.id = id;
            $.ajax({
                type: "POST",
                url: "/api/ManageOfAdminAPI/DeleteUser",
                data: JSON.stringify(data),
                contentType: "application/json; charset=utf-8",
                dataType: "json",
                success: function (result) {
                    alert(result);
                }
            });
        }
    }

    var index = 0;
    var flag = false;
    $(document).ready(function () {
        //click vào row của datatable
        $('#loadsubject tbody').on('click', 'tr', function () {
            flag = true;
            var table = $('#loadsubject').DataTable();
            var rowCurrent = $(this);
            console.log("index", rowCurrent);
            index = rowCurrent[0].sectionRowIndex;
            console.log("rowCurrent", index);
            if ($(this).hasClass('selected')) {
                $(this).removeClass('selected');
            }
            else {
                table.$('tr.selected').removeClass('selected');
                $(this).addClass('selected');
            }

        });
        //xóa 1 row datatable
        $('#btnDel').click(function () {
            var table = $('#loadsubject').DataTable();
            console.log("aaa", table.rows({ selected: true }).data());
            var rowData = table.rows({ selected: true }).data()[index]['id'];
            console.log("rowData", rowData);
            if (flag == true) {
                DeleteUser(rowData);
                table.row('.selected').remove().draw(false);
            }
        });
        $('body').on('click', '#btnEdit', function (e) {
            var table = $('#loadsubject').DataTable();
            var name = table.rows({ selected: true }).data()[index]['name'];
            alert(name);
            $("input[id='EditNameSub_Eb']").val(name);
            $("#EditSub").show();
        });
        $("#btnSaveEdit").click(function () {
            var table = $('#loadsubject').DataTable();
            var id = table.rows({ selected: true }).data()[index]['id'];
            var name = table.rows({ selected: true }).data()[index]['name'];
            var data = {};
            data.id = id;
            data.name = name;
            $.ajax({
                type: "POST",
                url: "/api/EbookAPI/EditSubjectById",
                data: JSON.stringify(data),
                contentType: "application/json; charset=utf-8",
                dataType: "json",
                success: function (result) {
                    if (result.isSuccess) {
                        alert("Sửa thành công.");
                        //add row datatable
                        var t = $('#loadsubject').DataTable();
                        //t.row.edit({
                        //    "id": id,
                        //    "name": name
                        //}).draw();
                        alert(index);
                        t.row(index).edit({
                            name: name
                        });
                    } else {
                        alert("Sửa không thành công.");
                    }
                }
            });
        });
    });
</script>
<!--Tạo môn học-->
<script>
    $.ajax({
        type: "GET",
        url: "/api/ManageOfAdminAPI/GetSubjectThesis",
        contentType: "application/json; charset=utf-8",
        dataType: "json",
        success: function (result) {

            $.each(result, function (i, v) {
                //alert(v.name);
                //load các môn của ebook lên thanh danh mục bên trái
                //$("#loadSubjectOfEbook").append("<li id='ebook_" + v.id + "'><a>" + v.name + "</a></li >");
                $("#loadSubjectOfEbook").append("<option value='"+v.id+"'>" + v.name + "</option>");
            });
        }
    });
      $(document).ready(function () {
        //Upload dữ liệu vào Ebook
          $("#addUser").click(function () {

            var data = new FormData();
              data.append("email", $("#email").val());
              data.append("password", $("#password").val());
              data.append("role", $("#role").val());
              data.append("name", $("#name").val());
              data.append("studentid", $("#studentid").val());
              data.append("classid", $("#classid").val());
              //data.append("classid",@Session["user_id"]);
             // data.append("subid", $("#loadSubjectOfEbook option:selected").val());


            console.log(data);
            $.ajax({

                type: "POST",
                url: "/api/ManageOfAdminAPI/AddUsers",
                dataType: "json",
                contentType: false,
                processData: false,
                data: data,

                //contentType: "application/json; charset=utf-8",
                //dataType: "json",
                success: function (result) {
                    if (result.isSuccess) {
                        alert("Tạo môn thành công.");
                        console.log("aaa", result);
                        //add row datatable
                        var t = $('#loadsubject').DataTable();
                        t.row.add({
                            "id": result.id,
                            "username": result.userName,
                            "title": result.title,
                            "password": result.passWord,
                            "role_id": result.role_Id,
                            "fullname": result.fullName,
                            "mssv": result.mssv,
                            "class_id": result.class_Id
                        }).draw();
                    } else {
                        alert("User đã tồn tại! Vui lòng đặt tên khác.");
                    }
                }
            });

        });


    });

</script>
<!--Sửa môn học-->
<script>
    $(document).ready(function () {

        //Khi bàn phím được nhấn và thả ra thì sẽ chạy phương thức này
        $('#submit_addUser').validate({
            rules: {
                email: {
                    required: true,
                    email: true
                },
                password:
                    {
                        required: true
                    },
                role: {
                    number: true,
                    minlength: 1,
                    maxlength: 1,
                    required: true
                },
                name: {
                    required: true
                },
                mssv: {
                    required: true,
                    number: true,
                    minlength: 8,
                    maxlength: 8,
                },
                malop: {
                    required: true
                }

            },
            messages: {
                email: {
                    required: "Bạn phải nhập email",
                    email: "Email không hợp lệ"
                },
                password: {
                    required: "Bạn phải nhập mật khẩu"
                },
                role: {
                    number: "Số điện thoại phải là số",
                    minlength: "Số điện thoại dài ít nhất 1 số",
                    maxlength: "Số điện thoại không quá 1 số",
                    required: "Bạn phải nhập mã quyền"
                },
                name:
                    {
                        required: "Bạn phải nhập họ tên"
                    },
                mssv: {
                    required: "Bạn phải nhập MSSV",
                    number: "MSSV phải là số",
                    minlength: "MSSV dài ít nhất 8 ký tự",
                    maxlength: "MSSV dài nhiều nhất 8 ký tự"
                },
                malop: {
                    required: "Bạn phải nhập mã lớp"
                }
            }
        });
    });
</script>
<!--Load môn học-->
<script>
    //load môn học ebook
    $(document).ready(function () {
        $.ajax({
            type: "GET",
            url: "/api/ManageOfAdminAPI/GetUsers",
            contentType: "application/json; charset=utf-8",
            dataType: "json",
            success: function (data) {
                InitTable(data);
            }
        });
        function InitTable(data) {
            $('#loadsubject').DataTable({
                data: data,
                "columns": [
                    { "data": "id" },
                    { "data": "username" },
                    { "data": "password" },
                    { "data": "role_id" },
                    { "data": "fullname" },
                    { "data": "mssv" },
                    { "data": "class_id" },

                ]
            });
        }
        function clearEditSub() {
            $("input[id='EditNameSub_Eb']").val("");
        }
        function clearAddSub() {
            $("input[id='AddNameSub_Eb']").val("");
        }
        $("#btn_closeX").click(function () {
            $("#EditSub").hide();
            clearEditSub();
        });
        $("#btn_closeEdit").click(function () {
            $("#EditSub").hide();
            clearEditSub();
        });
        //$("#btn_closeX1").click(function () {
        //    $("#AddSub").hide();
        //    clearAddSub();
        //});
        //$("#btn_closeAdd").click(function () {
        //    $("#AddSub").hide();
        //    clearAddSub();
        //});
    });

</script>
